The Database module loads data from a file and stores it in some attributes; the data in the file is organized on two columns, separated by 
a number of blanks.

The values stored in the file are encrypted; the application decrypts them when loading data from file, and encrypts them before saving them 
into a file.
